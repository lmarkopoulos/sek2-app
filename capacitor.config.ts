import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'gr.sek2.unionapp',
  appName: 'sek2-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
